template(name='allPlaylists')
  .box
    .box__header.box__header--with-border
      h3 Channel Playlists
    .box__body
      .box__container
        if playlistsCount
          ul.items__list
            each playlists
              +playlistItem
        else
          span.box__empty
            | No playlists
