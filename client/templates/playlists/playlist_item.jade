template(name='playlistItem')
  li
    .item__thumbnail
      if isDashboardPage
        .item__actions
          button.btn.btn-js-delete-playlist
            span.mdi.mdi-delete
          button.btn.btn-js-edit-playlist
            span.mdi.mdi-pencil
      a(href='{{pathFor route=route _id=_id _userId=ownerId}}')
        img(alt='Playlist' src='/playlist-img-default.jpg')
        .item__thumbnail__cover
          div #{itemsListCount}
            span recordings
    .item__title
      a(href='{{pathFor route=route _id=_id _userId=ownerId}}')
        | #{title}
    .item__metadata
      | #{owner}
    .item__date
      | #{createdAt}
