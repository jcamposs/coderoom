template(name="layout")
  .room__header
    .room__header__logo
      a(href='/')
        img(src='/code-icon.png')
        span CodeRoom

  main
    +yield

  if loading
    .overlay__container
      +spinner
