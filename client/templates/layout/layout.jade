template(name="layout")
  .room__header
    .room__header__logo
      a(href='/')
        img(src='/code-icon.png')
        span CodeRoom

  .alerts
    each alerts
      +alert

  main
    +yield

  if loading
    .overlay__container
      +spinner
