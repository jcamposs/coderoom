template(name='media')
  .media__container
    .media__controls
      .badge__media
        if live
          .badge__mode.badge__mode--live
            if recording
              span.mdi.mdi-record-rec.mdi-18px
            | Live

        if uploading
          .badge__state
            span.mdi.mdi-autorenew.rotating.mdi-24px

      if live
        +mediaControls

    video#main-media(src='' autoplay)

    .media__screen__container
    
    #media__participants__container.media__participants__container
